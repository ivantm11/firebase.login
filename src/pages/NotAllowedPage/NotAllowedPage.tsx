import { FC } from 'react';

import styles from './NotAllowedPage.module.scss';

const NotAllowedPage: FC = () => {
  return <div className={styles.NotAllowedPage}>Not Allowed Page</div>;
};

export default NotAllowedPage;
